name: Deploy frontend to Firebase Hosting

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 12.3.4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm build
        env:
          VITE_FIREBASE_API_KEY: AIzaSyA6S0xq5iAZMNyGFuqKo3fH90_JO_xtjFc
          VITE_FIREBASE_AUTH_DOMAIN: animeascan.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: animeascan
          VITE_FIREBASE_STORAGE_BUCKET: animeascan.firebasestorage.app
          VITE_FIREBASE_MESSAGING_SENDER_ID: 875878871736
          VITE_FIREBASE_APP_ID: 1:875878871736:web:2d49ce65a3dd7abf38fa4d
          VITE_API_BASE_URL: https://anemiascan-backend.onrender.com

      - name: Deploy live site
        if: github.event_name == 'push'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANIMEASCAN }}
          projectId: animeascan
          channelId: live

      - name: Deploy pull request preview
        if: github.event_name == 'pull_request'
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANIMEASCAN }}
          projectId: animeascan
          channelId: pr-${{ github.event.pull_request.number }}
